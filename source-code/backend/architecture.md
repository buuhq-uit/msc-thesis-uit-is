## Backend System design

```text

                  ┌──────────────────────┐      ┌──────────────────────┐ 
                  │   services/rest      │      │   services/rest-app  │
                  │  (controllers,..)    │<-----│  (Spring Boot App)   │
                  └─────────┬────────────┘      └─────────-────────────┘
                            │                             
          ┌─────────────────┼──────────────--------───-┐──────----───────-┐
          │                 │                          │                  │
┌─────────▼─────────┐ ┌─────▼────-------───-─┐ ┌────-──▼─────────┐ ┌──────▼──────────┐
│    application    │ │ infrastructure       │ │ process-engine  │ │ rule-engine     │
│  (use cases)      │ │ (JPA, HTTP, adapters)│ │ (Flowable)      │ │ (Drools)        │
└─────────┬─────────┘ └─────-───┬─-------────┘ └───-─────┬───────┘ └───-─────┬───────┘
          │                     │                        │                   │ 
          └──────────────┬──────┴──────┬─────────-------─┘───────────────-───┘
                         ▼             ▼
                   ┌─────────────────-───────┐     ┌─────────────────-───────┐ 
                   │         domain          │     │         common          │
                   │ (models, rules, ports)  │     │ (other common logic)    │
                   └────────────────-────────┘     └────────────────-────────┘

```